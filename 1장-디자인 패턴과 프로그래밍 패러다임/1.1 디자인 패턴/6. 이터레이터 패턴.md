## **6\. 이터레이터 패턴**

> 컬렉션 구현 방법을 노출시키지 않고 그 집합체 안에 있는 모든 항목에 접근할 수 있게 해주는 방법을 제공하며, 이러한 기능을 집합체가 아닌 **이터레이터 객체**에서 책임진다.

### **▶️ Iterator를 쓰면**

컬렉션 내부 구조를 밖에 공개하지 않고도 외부에서 컬렉션 안의 모든 요소를 순회할 수 있고,

심지어 순회 로직 자체를 컬렉션 밖으로 분리할 수도 있다.

<br>

> **CODE: 식당과 팬케이크 하우스의 합병**

```
1️⃣ PancakeHouseMenu pancakeHouseMenu = new PancakeHouseMenu();
2️⃣ DinerMenu dinerMenu = new DinerMenu();

3️⃣ Waitress waitress = new Waitress(pancakeHouseMenu, dinerMenu);
4️⃣ waitress.printMenu();
```

> **MenuItem**

```
public class MenuItem {
    String name;
    String description;
    boolean vegetarian;
    double price;
    
    public MenuItem(String name, String description, boolean vegetarian, double price) {
         this.name = name;
         this.description = description;
         this.vegetarian = vegetarian;
         this.price = price
    }
    
    // public String getName() { return name; }
}
```

>  **Iterator 선언**

```
public interface Iterator {
    boolean hasNext();
    Object next();
}
```

```
public class DinerMenuIterator implements Iterator {
    MenuItem[] items;
    int position = 0;
    
    public DinerMenuIterator(MenuItem[] items) {
    	this.items = items;
    }
    
    public Object next() {
    	MenuItem menuItem = items[position];
        position = position + 1;
        return menuItem;
    }
    
    public boolean hasNext() {
    	if (position >= items.length || items[position] == null) {
            return false;
        } else {
            return true;
        }
    }
}
```

같은 방식으로 PancakeMenuIterator도 만들어준다.

> **Menu**

```
// 식당 메뉴
public class DinerMenu {
    static final int MAX_ITEMS = 6;
    int numberOfItems = 0;
    MenuItem[] menuItems;                 // 배열에 메뉴 저장
    
    // 생성자
    
    // addItem 메소드
    
    public Iterator createIterator() {    // 각 메뉴 항목에 대한 iterator를 만들어줌
        return new DinerMenuIterator(menuItems);
    }
}

// 팬케이크 메뉴
```

> **Waitress**

식당과 팬케이크 가게를 병합한, 새로운 가게의 메뉴를 소개하는 종업원 클래스다.

```
public class Waitress {
    PancakeHouseMenu pancakeHouseMenu;
    DinerMenu dinerMenu;
    
    public Waitress(PancakeHouseMenu pancakeHouseMenu, DinerMenu dinerMenu) {
    	this.pancakeHouseMenu = pancakeHouseMenu;
        this.dinerMenu = dinerMenu;
    }
    
    public void printMenu() {
    	Iterator pancakeIterator = pancakeHouseMenu.createIterator();
        Iterator dinerIterator = dinerMenu.createIterator();
        System.out.println("메뉴\n---\n아침 메뉴");
        printMenu(pancakeIterator);
        System.out.println("\n점심 메뉴");
        printMenu(dinerIterator);
    }
    
    public void printMenu(Iterator) {
    	while (iterator.hasNext()) {
        	MenuItem menuItem = (MenuItem)iterator.next();
            // System.out.println();
        }
    }
}
```

### **✨ 자바에 내장된 Iterator 인터페이스를 사용하자 !**

ArrayList 내에는 이미 iterator()라는 메소드가 있다.

```
// 팬케이크 
public Iterator createIterator() {
    return menuItems.iterator();
}

// 식당
public class DinerMenuIterator implements Iterator {
    MenuItem[] list;
    int position = 0;
    
    public DinerMenuIterator(MenuItem[] list) {
    	this.list = list;
    }
    
    // public Object next() {};
    // public boolean hasNext() {};
    
    public void remove() {    // 크기가 고정된 배열이기 때문에
    	// if (position <= 0) {
        //  	throw new IllegalStateException("next를 ")
        // }
        
        // ...
    }
}
```

```
public class Waitress {
    Menu pancakeHouseMenu;
    Menu dinerMenu;
    
    // ...
}
```

PancakeHouseMenu와 DinerMenu 클래스에는 **공통적으로 Menu 인터페이스 구현**

**→**  waitress가 구상 메뉴 클래스에 의존하는 문제를 해결할 수 있음
